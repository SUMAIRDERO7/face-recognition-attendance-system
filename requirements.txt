opencv-python-headless>=4.9.0
numpy>=1.26.0
streamlit>=1.38.0
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional but strongly recommended for real accuracy.
# Requires dlib, which ships only as a C++ source distribution and can take
# 30+ minutes to compile. See the README's "Which Encoder Am I Getting?".
# face_recognition>=1.3.0
